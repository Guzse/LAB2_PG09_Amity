import React from "react";

function Sidebar() {

    const safezoneList = []

    for (let i = 0;i++; safezoneList.length) {
        list.push(<a href="#">{safezoneList}</a>)
    }

    return ( 
        <div>
            <p>Sidebar</p>
            <div className="safezoneList">
                {serverlist}
                <a href="#">{createSafezone}</a>
            </div>
            <div className="usersAndOptions"></div>
        </div>
     );
}

export default Sidebar;